# Django
Django==6.0.6

# PostgreSQL adapter
psycopg2-binary==2.9.12

# Environment variables (.env)
python-decouple==3.8

# PDF export (timetable) — reportlab chosen over weasyprint:
# pure Python, no system Cairo/Pango dependencies, simpler deployment.
reportlab==5.0.0

# Excel export (timetable)
openpyxl==3.1.5
