# scheduling/engine — pure-Python scheduling engine package.
# The only file in this package that may import Django ORM is models_io.py.
# algorithm.py, constraints.py, and data_types.py are framework-agnostic
# and must remain importable without a running Django application.
